#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../wordpos/bin/wordpos-cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../wordpos/bin/wordpos-cli.js" "$@"
  ret=$?
fi
exit $ret
